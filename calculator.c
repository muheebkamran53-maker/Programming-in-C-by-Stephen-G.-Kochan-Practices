#include<stdio.h>
int main (void)
{
    float value, S;
    char operator;
    
    printf("Begin the calculation: \n");
    printf("Enter any value with an operator:\n");
    do
    {
        scanf("%f %c",&value,operator);
        switch(operator)

        case 'S':
        S = value;
        
        
        
    }
    while(value != E);
    
    
}